const ErrorMessage = ({ message }) => {
  return (
    <div className="text-white bg-slate-950 rounded-lg p-4 w-80 text-center font-bold">
      {message}📛
    </div>
  );
};

export default ErrorMessage;
