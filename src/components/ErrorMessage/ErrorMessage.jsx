const ErrorMessage = ({ message }) => {
  return (
    <div className="text-white bg-slate-900 rounded-lg p-6 w-72 text-center font-normal">
      {message}📛
    </div>
  );
};

export default ErrorMessage;
