const ErrorMessage = ({ message }) => {
  return (
    <div className="text-white bg-slate-950 rounded-lg p-5 w-72 text-center font-normal">
      {message}📛
    </div>
  );
};

export default ErrorMessage;
